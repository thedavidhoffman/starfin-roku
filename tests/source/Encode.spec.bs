namespace tests

    @suite("Encoding helpers")
    class EncodeTests extends rooibos.BaseTestSuite

        @describe("Encode_Base64")

        @it("encodes ASCII text and empty values")
        function _()
            m.assertEqual(Encode_Base64("Starfin"), "U3RhcmZpbg==")
            m.assertEqual(Encode_Base64(""), "")
        end function

        @describe("Encode_Url")

        @it("preserves URL-unreserved characters")
        function _()
            m.assertEqual(Encode_Url("AZaz09-._~"), "AZaz09-._~")
        end function

        @it("percent-encodes reserved characters with uppercase hex")
        function _()
            m.assertEqual(Encode_Url("a b/c?d=e&f"), "a%20b%2Fc%3Fd%3De%26f")
        end function

        @it("encodes percent signs instead of treating them as existing escapes")
        function _()
            m.assertEqual(Encode_Url("already%20encoded"), "already%2520encoded")
        end function

    end class

end namespace
