namespace MediaPlaybackPolicy

    '-------------------------------------------------------------------------------
    ' MediaPlaybackPolicy.TracksProgress
    '-------------------------------------------------------------------------------
    function TracksProgress(item as dynamic) as boolean
        if item = invalid then return true
        return LCase(Format.SafeString(item.Type, "")) <> "musicvideo"
    end function

end namespace
